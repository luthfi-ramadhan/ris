---
title: How To Use Tmux
author: Mgs. M. Luthfi Ramadhan
tags:
  - computing
  - deep learning
  - training
  - terminal
---

# 🧩 tmux Tutorial (Simple & Practical)

`tmux` is a terminal multiplexer. In short:  
👉 **You can run multiple terminal sessions and keep them alive even if SSH disconnects.**

This tutorial focuses only on the **most important things**:
- Create session
- Detach
- Attach
- Delete session
- Scroll terminal output

---

## 1️⃣ Start a New tmux Session

### Create a session with a name

`tmux new -s mysession`


You are now **inside tmux**.

---

## 2️⃣ Detach from a Session (Leave tmux running)

Detach means:  
❗ **Session keeps running in background**

### Key combination
`Ctrl + b then d`

---

## 3️⃣ List Existing Sessions
`tmux ls`

---

## 4️⃣ Attach to an Existing Session
`tmux a -t mysession`

---

## 5️⃣ Kill / Delete a Session
### Kill a specific session by name
`tmux kill-session -t mysession`
### Kill **all** tmux sessions
`tmux kill-server`

⚠️ This will terminate all running tmux sessions.

---

## 6️⃣ Scroll Terminal Output (Very Important!)
By default, mouse scrolling **does NOT work** in tmux.
### Enter scroll (copy) mode
`Ctrl + b then [`
Now you can:
- Scroll with **Arrow keys**
- `Page Up` / `Page Down`
- Use mouse wheel (if mouse enabled)

### Exit scroll mode
`q`